docker build -t autologininternet:1.0 .

docker save -o autoLoginInternet.tar ae1f14828d8b

docker run -d \
  --name autoLoginInternet \
  -v /root/dockerImages/autologininternet/config.json:/app/config.json:ro \
  -v /root/dockerImages/autologininternet/login.log:/app/login.log \
  --restart unless-stopped \
  3e7564559130
